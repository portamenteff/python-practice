pyg = 'ay'

original = raw_input('Enter a word:')
word = original.lower()
if len(original) > 0 and original.isalpha():
    print original
else:
    print 'empty'
first = word[0]
new_word = word[1:] + first + pyg
print new_word
